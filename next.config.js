/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['thumb.tildacdn.com', 'pixabay.com', 'alturl.com']
  }
  
}

module.exports = nextConfig
